#!/bin/bash

. ~/.settings

# xfce terminal
#Terminal --role autoterm --tab --tab --tab --geometry=$AUTOTERM_GEOMETRY2

# armin's rxvt
# export LC_ALL=en_US.ISO-8859-15

# N=${1:-6}
# for i in `seq 1 $N`
# do
#     rxvt -name autorxvt -geometry $AUTOTERM_GEOMETRY2 &
# done

# gnome terminal
DIR=~/env/src/gnome-terminal-hack

export TERM=xterm-256color
export GTK2_RC_FILES=$DIR/gtkrc
export LD_PRELOAD=$DIR/bottomtab.so
gnome-terminal --load-config $DIR/autoterm-config
